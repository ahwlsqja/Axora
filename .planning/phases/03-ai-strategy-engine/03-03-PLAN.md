---
phase: 03-ai-strategy-engine
plan: 03
type: execute
wave: 3
depends_on: ["03-02"]
files_modified:
  - src/components/strategy/ProposalCard.tsx
  - src/components/strategy/OrderPreview.tsx
  - src/components/strategy/ParameterAdjuster.tsx
  - src/components/strategy/RiskWarning.tsx
  - src/components/intent/IntentConfirmation.tsx
autonomous: false

must_haves:
  truths:
    - "User sees a proposal card showing orders, prices, total capital, and worst-case outcome"
    - "User can adjust price range, amount, and split count on the proposal"
    - "Risk warnings and errors are displayed when validation fails"
    - "Intent confirmation triggers strategy generation and shows proposal"
  artifacts:
    - path: "src/components/strategy/ProposalCard.tsx"
      provides: "Main proposal display card with strategy summary"
      min_lines: 50
    - path: "src/components/strategy/OrderPreview.tsx"
      provides: "Order table/list showing individual orders in the strategy"
      min_lines: 30
    - path: "src/components/strategy/ParameterAdjuster.tsx"
      provides: "Controls for adjusting splitCount, priceRange, totalAmount"
      min_lines: 40
    - path: "src/components/strategy/RiskWarning.tsx"
      provides: "Warning/error display from validation results"
      min_lines: 20
  key_links:
    - from: "src/components/strategy/ProposalCard.tsx"
      to: "src/stores/strategyStore.ts"
      via: "useStrategyStore for proposal data"
      pattern: "useStrategyStore"
    - from: "src/components/strategy/ParameterAdjuster.tsx"
      to: "src/stores/strategyStore.ts"
      via: "adjustSplitCount, adjustPriceRange, adjustTotalAmount actions"
      pattern: "adjust(SplitCount|PriceRange|TotalAmount)"
    - from: "src/components/intent/IntentConfirmation.tsx"
      to: "src/hooks/useStrategy.ts"
      via: "generate() call on proceed"
      pattern: "useStrategy.*generate"
    - from: "src/components/strategy/RiskWarning.tsx"
      to: "src/stores/strategyStore.ts"
      via: "validation.warnings and validation.errors"
      pattern: "validation\\.(warnings|errors)"
---

<objective>
Build the strategy proposal UI components and wire IntentConfirmation to trigger strategy generation, completing the user-facing flow from intent to adjustable proposal.

Purpose: Deliver the visual layer that lets users see, understand, and adjust AI-generated strategy proposals before execution (Phase 4).
Output: 4 new strategy components + updated IntentConfirmation. User can go from intent selection to viewing and adjusting a strategy proposal.
</objective>

<execution_context>
@/home/ahwlsqja/.claude/get-shit-done/workflows/execute-plan.md
@/home/ahwlsqja/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-ai-strategy-engine/03-RESEARCH.md
@.planning/phases/03-ai-strategy-engine/03-01-SUMMARY.md
@.planning/phases/03-ai-strategy-engine/03-02-SUMMARY.md
@src/components/intent/IntentConfirmation.tsx
@src/components/intent/IntentSection.tsx
@src/stores/intentStore.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create strategy proposal UI components</name>
  <files>
    src/components/strategy/ProposalCard.tsx
    src/components/strategy/OrderPreview.tsx
    src/components/strategy/ParameterAdjuster.tsx
    src/components/strategy/RiskWarning.tsx
  </files>
  <action>
1. Create `src/components/strategy/RiskWarning.tsx`:
   - Props: `warnings: string[]`, `errors: string[]`
   - Render nothing if both arrays empty
   - Errors: red background card with red text, each error as a bullet point, heading "Strategy Rejected" (AI-05: refuses strategies exceeding thresholds)
   - Warnings: yellow/amber background card with amber text, each warning as bullet, heading "Warnings"
   - Tailwind styling consistent with existing app (rounded-xl, p-4 pattern from intent components)

2. Create `src/components/strategy/OrderPreview.tsx`:
   - Props: `orders: StrategyOrder[]`, `baseDenom: string`, `quoteDenom: string`
   - Render a compact table/list of orders:
     - Columns: # (index), Side (buy/sell with color coding: green/red), Price (in quoteDenom), Quantity (in baseDenom), % of Total
     - Each row is a single order from the proposal
   - Footer row: total quantity, total capital
   - Use Tailwind table styling: `divide-y`, text-sm, monospace for numbers
   - If orders array is empty, show "No orders" placeholder

3. Create `src/components/strategy/ParameterAdjuster.tsx`:
   - Import `useStrategyStore` for adjustment actions
   - Read current proposal from store
   - Three adjustment controls:
     a. **Split Count**: number input (or Radix Slider if available) with min=1, max=20, step=1. On change: call `adjustSplitCount(value)`
     b. **Price Range**: two number inputs (min and max) with step appropriate for the market. On change: call `adjustPriceRange(min, max)`. Show current market price as reference.
     c. **Total Amount**: number input for total capital in quote currency. On change: call `adjustTotalAmount(value)`
   - Each control has a label and current value display
   - Debounce adjustments by 300ms to avoid excessive recalculations during slider drag
   - Use Tailwind form styling: input with border, rounded, focus ring
   - Render nothing if no proposal in store

4. Create `src/components/strategy/ProposalCard.tsx`:
   - Import `useStrategyStore` for proposal + validation state
   - Import OrderPreview, ParameterAdjuster, RiskWarning
   - If no proposal, render nothing (parent controls visibility)
   - If isGenerating, show loading spinner/skeleton with "Generating strategy..." text
   - If error, show error message with retry option
   - Card layout (Tailwind: rounded-xl border shadow-sm p-6):
     a. Header: strategy type badge (colored by type), market pair (e.g., "INJ/USDT")
     b. Summary row: total capital, split count, price range (min-max)
     c. Reasoning: AI's explanation of why this strategy was structured this way
     d. OrderPreview component with the orders array
     e. ParameterAdjuster component
     f. RiskWarning component with validation warnings/errors
     g. Worst-case outcome: displayed in a muted/warning box
     h. Footer: "Proceed to Execute" button (disabled if validation.valid is false). This button is a placeholder for Phase 4 -- for now, show a sonner toast "Execution coming in Phase 4" on click.
   - Korean labels with English fallback (consistent with existing intent cards pattern using titleKo/titleEn)
  </action>
  <verify>
    Run `npx tsc --noEmit` to confirm all components compile. Verify each component file exists and has correct imports.
  </verify>
  <done>
    Four strategy components created: ProposalCard (main card with all sections), OrderPreview (order table), ParameterAdjuster (3 adjustment controls with debounce), RiskWarning (error/warning display). All read from strategyStore. ProposalCard composes the other three. Proceed button is Phase 4 placeholder.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire IntentConfirmation to trigger strategy generation and show ProposalCard</name>
  <files>
    src/components/intent/IntentConfirmation.tsx
  </files>
  <action>
1. Update `src/components/intent/IntentConfirmation.tsx`:
   - Read existing file first to understand current structure
   - Import `useStrategy` from '@/hooks/useStrategy'
   - Import `useStrategyStore` from '@/stores/strategyStore'
   - Import `ProposalCard` from '@/components/strategy/ProposalCard'
   - Import `useSupportedMarkets` from '@/hooks/useMarketData'

2. Modify the "Continue" / proceed action:
   - When wallet is connected and user clicks proceed:
     a. Get the first supported market ID (INJ/USDT for MVP) from `useSupportedMarkets()`
     b. Call `generate({ presetId, freeText, source, marketId })` from useStrategy hook
   - While generating: show loading state on button (spinner + "Generating...")
   - The ProposalCard renders below the confirmation when proposal exists in store

3. Add ProposalCard below the existing confirmation card:
   - Conditionally render `<ProposalCard />` when `useStrategyStore().proposal` is not null
   - The ProposalCard handles its own loading/error states internally

4. Add a "Reset" action that calls both `intentStore.clear()` and `strategyStore.reset()` to go back to intent selection

5. Handle the case where market data is loading: show a brief "Loading market data..." state before the generate button is active. If supported markets fail to load, show error message.
  </action>
  <verify>
    Run `npx tsc --noEmit` to confirm IntentConfirmation compiles with new imports. Run `npm run build` to verify the full build succeeds (catches any SSR/import issues).
  </verify>
  <done>
    IntentConfirmation triggers strategy generation on proceed (with wallet connected), passes intent data + market ID to the API, and renders ProposalCard below when proposal is available. Reset clears both intent and strategy stores. Full build passes.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify end-to-end AI strategy engine flow</name>
  <action>
    Human verifies the complete AI strategy engine flow: intent selection/free-text -> wallet gate -> strategy generation via LLM -> proposal card with orders, parameters, risk warnings -> parameter adjustment (client-side recalculation).

    Steps:
    1. Ensure OPENAI_API_KEY is set in .env.local
    2. Run `npm run dev`
    3. Visit http://localhost:3000
    4. Select a preset intent card (e.g., "DCA into INJ")
    5. Click "Continue" (connect wallet if needed)
    6. Verify: loading state appears while AI generates
    7. Verify: ProposalCard appears with strategy type, orders table, total capital, worst-case outcome
    8. Verify: Adjust split count slider/input -- orders recalculate without new API call
    9. Verify: Adjust price range -- orders redistribute
    10. Verify: If proposal has warnings, yellow warning box appears
    11. Try free-text: type "I want to buy 100 USDT worth of INJ at a 10% discount" and proceed
    12. Verify: AI generates appropriate limit buy strategy from free-text
    13. Verify: "Proceed to Execute" button shows Phase 4 placeholder toast
  </action>
  <verify>All 13 verification steps pass</verify>
  <done>User has confirmed the end-to-end strategy generation and proposal display flow works correctly</done>
</task>

</tasks>

<verification>
- All strategy components render without React errors
- ProposalCard shows all required information: orders, prices, total capital, worst-case
- Parameter adjustments update orders client-side (no network request visible in devtools)
- Risk warnings appear when validation returns warnings/errors
- IntentConfirmation -> strategy generation flow works end-to-end
- Free-text input produces appropriate strategy proposal
- Full build (`npm run build`) passes
</verification>

<success_criteria>
User can go from selecting an intent to viewing a complete, adjustable strategy proposal. The proposal shows orders, prices, total capital, worst-case outcome. Parameters (split count, price range, amount) can be adjusted without re-calling AI. Validation warnings and risk threshold errors are visible. This satisfies requirements AI-01 through AI-05.
</success_criteria>

<output>
After completion, create `.planning/phases/03-ai-strategy-engine/03-03-SUMMARY.md`
</output>
