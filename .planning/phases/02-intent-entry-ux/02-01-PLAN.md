---
phase: 02-intent-entry-ux
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/constants/intents.ts
  - src/types/index.ts
  - src/stores/intentStore.ts
  - src/components/intent/IntentCard.tsx
  - src/components/intent/IntentCardGrid.tsx
  - src/components/intent/IntentSection.tsx
  - src/components/dashboard/DashboardSection.tsx
  - src/app/page.tsx
autonomous: true

must_haves:
  truths:
    - "User sees 6 goal-based intent cards on the landing screen without connecting a wallet"
    - "User can select a preset intent card and it visually highlights as selected"
    - "Intent cards display Korean primary text and English secondary text"
    - "Existing dashboard (balances, agent account) still renders correctly when wallet is connected"
  artifacts:
    - path: "src/constants/intents.ts"
      provides: "Preset intent definitions array"
      exports: ["PresetIntent", "PRESET_INTENTS"]
    - path: "src/stores/intentStore.ts"
      provides: "Intent selection state management"
      exports: ["useIntentStore"]
    - path: "src/types/index.ts"
      provides: "IntentSource type added to existing types"
      contains: "IntentSource"
    - path: "src/components/intent/IntentCard.tsx"
      provides: "Single intent card component"
      exports: ["IntentCard"]
    - path: "src/components/intent/IntentCardGrid.tsx"
      provides: "Grid layout of all intent cards"
      exports: ["IntentCardGrid"]
    - path: "src/components/intent/IntentSection.tsx"
      provides: "Section combining cards (free-text added in plan 02)"
      exports: ["IntentSection"]
    - path: "src/components/dashboard/DashboardSection.tsx"
      provides: "Extracted dashboard content from page.tsx"
      exports: ["DashboardSection"]
    - path: "src/app/page.tsx"
      provides: "Restructured landing: intent-first, dashboard-second"
      min_lines: 15
  key_links:
    - from: "src/components/intent/IntentCardGrid.tsx"
      to: "src/constants/intents.ts"
      via: "imports PRESET_INTENTS array"
      pattern: "import.*PRESET_INTENTS.*from.*constants/intents"
    - from: "src/components/intent/IntentCard.tsx"
      to: "src/stores/intentStore.ts"
      via: "calls selectPreset on click"
      pattern: "selectPreset|useIntentStore"
    - from: "src/app/page.tsx"
      to: "src/components/intent/IntentSection.tsx"
      via: "renders IntentSection unconditionally"
      pattern: "<IntentSection"
    - from: "src/app/page.tsx"
      to: "src/components/dashboard/DashboardSection.tsx"
      via: "renders DashboardSection conditionally when connected"
      pattern: "status.*connected.*DashboardSection"
---

<objective>
Create the intent-first landing page with 6 preset goal-based intent cards that are always visible regardless of wallet connection status. Extract the existing dashboard into a separate component rendered conditionally below the intent section.

Purpose: Fulfills ONBR-01 (goal-based entry points visible without wallet) and INTNT-01 (preset intent card selection). Inverts the current wallet-gated UX into an intent-first browsable experience.
Output: Intent data model, store, card components, restructured landing page.
</objective>

<execution_context>
@/home/ahwlsqja/.claude/get-shit-done/workflows/execute-plan.md
@/home/ahwlsqja/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-intent-entry-ux/02-RESEARCH.md
@src/app/page.tsx
@src/stores/walletStore.ts
@src/types/index.ts
@src/components/dashboard/BalanceCard.tsx
@src/hooks/useWallet.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create intent data model, types, and Zustand store</name>
  <files>src/constants/intents.ts, src/types/index.ts, src/stores/intentStore.ts</files>
  <action>
1. Create `src/constants/intents.ts` with:
   - Export `PresetIntent` interface: `{ id: string, titleKo: string, titleEn: string, descriptionKo: string, descriptionEn: string, icon: string, category: 'accumulate' | 'protect' | 'profit' | 'entry' }`
   - Export `PRESET_INTENTS: PresetIntent[]` with 6 intents:
     1. `dca-inj` -- INJ 분할매수 / DCA into INJ (category: accumulate, icon: layers emoji or text)
     2. `stop-loss` -- 손절 설정 / Set stop-loss (category: protect)
     3. `scale-in` -- 눌림목 매수 / Scale into dip (category: accumulate)
     4. `take-profit` -- 일부 익절 / Take partial profit (category: profit)
     5. `limit-buy` -- 지정가 매수 / Limit buy at discount (category: entry)
     6. `range-accumulate` -- 구간 매집 / Range accumulation (category: accumulate)
   - Each intent has Korean-first bilingual labels with short descriptions

2. Add to `src/types/index.ts`:
   - Export `type IntentSource = 'preset' | 'freetext'`
   - Do NOT remove existing types (WalletType, ConnectionStatus, OnboardingStep, TokenBalance)

3. Create `src/stores/intentStore.ts` following the existing walletStore pattern (Zustand create with typed interface):
   - State: `selectedPresetId: string | null`, `freeText: string`, `source: IntentSource | null`
   - Actions: `selectPreset(presetId: string)` -- sets preset, clears freeText, sets source to 'preset'
   - Actions: `setFreeText(text: string)` -- updates freeText field
   - Actions: `submitFreeText()` -- if freeText.trim() is non-empty, clears preset, sets source to 'freetext'
   - Actions: `clear()` -- resets all state to initial
   - Import IntentSource from `@/types`
  </action>
  <verify>Run `npx tsc --noEmit` to confirm no type errors. Verify exports: `grep -r "PRESET_INTENTS\|useIntentStore\|IntentSource" src/`</verify>
  <done>6 preset intents defined as typed constants, IntentSource type exported, intentStore created with select/clear actions matching walletStore patterns</done>
</task>

<task type="auto">
  <name>Task 2: Build intent card components and restructure landing page</name>
  <files>src/components/intent/IntentCard.tsx, src/components/intent/IntentCardGrid.tsx, src/components/intent/IntentSection.tsx, src/components/dashboard/DashboardSection.tsx, src/app/page.tsx</files>
  <action>
1. Create `src/components/intent/IntentCard.tsx`:
   - 'use client' component
   - Props: `intent: PresetIntent`, `selected: boolean`, `onSelect: (id: string) => void`
   - Render as a `<button>` with full width, text-left
   - Match existing card style: `rounded-2xl border p-5` base
   - Selected state: `border-blue-300 bg-blue-50 ring-2 ring-blue-200`
   - Default state: `border-gray-100 bg-white shadow-sm hover:border-gray-200 hover:bg-gray-50`
   - Show icon (use emoji mapping: layers=📊, shield=🛡️, entry=🎯, profit=💰, accumulate=📈), titleKo (text-sm font-medium text-gray-900), titleEn (text-xs text-gray-500), descriptionKo (text-xs text-gray-400 mt-2)
   - transition-colors for hover smoothness

2. Create `src/components/intent/IntentCardGrid.tsx`:
   - 'use client' component
   - Import PRESET_INTENTS from `@/constants/intents`
   - Import useIntentStore
   - Render a responsive grid: `grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4`
   - Map PRESET_INTENTS to IntentCard components
   - Pass `selected={selectedPresetId === intent.id}` and `onSelect={selectPreset}`

3. Create `src/components/intent/IntentSection.tsx`:
   - 'use client' component
   - Section header: "무엇을 하고 싶으세요?" (text-xl font-semibold text-gray-900) with subtitle "What would you like to do?" (text-sm text-gray-500)
   - Render IntentCardGrid below the header
   - Add a spacer/divider (`mb-4`) between header and grid
   - NOTE: FreeTextInput will be added here in plan 02-02. Leave a comment marking where it goes: `{/* Free-text input added in 02-02 */}`

4. Create `src/components/dashboard/DashboardSection.tsx`:
   - 'use client' component
   - Extract the ENTIRE connected-state content from current page.tsx into this component
   - Props: `address: string` (required -- only rendered when connected)
   - Move AgentSubaccountCard and SetupAgentCard as private components inside this file (or keep inline)
   - Import and use useBalances, useSubaccountBalance hooks
   - Preserve all existing dashboard behavior exactly: agent account card, balance loading spinner, BalanceCardList
   - Add a visual separator above: `mt-12 pt-8 border-t border-gray-100`

5. Restructure `src/app/page.tsx`:
   - Keep 'use client'
   - Import IntentSection and DashboardSection
   - Render IntentSection UNCONDITIONALLY (no wallet check)
   - Render DashboardSection CONDITIONALLY: `{status === 'connected' && address && <DashboardSection address={address} />}`
   - Remove the old "Connect your wallet" placeholder entirely
   - Remove AgentSubaccountCard and SetupAgentCard from this file (moved to DashboardSection)
   - The page should be clean: ~15-25 lines
  </action>
  <verify>Run `npx tsc --noEmit` for type checking. Run `npm run build` to confirm the page compiles. Start dev server (`npm run dev`) and verify: (1) intent cards visible without wallet, (2) clicking a card highlights it, (3) connecting wallet shows dashboard below cards.</verify>
  <done>Landing page shows 6 intent cards without wallet connection. Cards are selectable with visual highlight. Dashboard renders below when wallet connected. No regressions in balance display or agent account card.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with zero errors
2. `npm run build` completes successfully
3. Without wallet connected: 6 intent cards visible on landing page, bilingual text shown
4. Clicking a card: visual selection state (blue border/bg), clicking another card switches selection
5. With wallet connected: dashboard section appears below intent cards with balances and agent account
6. No broken imports or missing components
</verification>

<success_criteria>
- 6 preset intent cards render on landing page without wallet connection (ONBR-01)
- Cards are clickable and show selection state (INTNT-01)
- Korean primary + English secondary text on each card
- Existing dashboard functionality preserved when wallet connected
- Intent selection stored in Zustand intentStore
- Build passes with no errors
</success_criteria>

<output>
After completion, create `.planning/phases/02-intent-entry-ux/02-01-SUMMARY.md`
</output>
