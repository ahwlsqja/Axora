---
phase: 02-intent-entry-ux
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/components/intent/FreeTextInput.tsx
  - src/components/intent/IntentSection.tsx
  - src/components/intent/IntentConfirmation.tsx
  - src/app/page.tsx
autonomous: true

must_haves:
  truths:
    - "User can type a free-text natural language intent and submit it"
    - "User sees a confirmation state after selecting a preset or submitting free text"
    - "User is prompted to connect wallet only when clicking proceed/execute, not while browsing"
    - "User can clear their selection and start over"
  artifacts:
    - path: "src/components/intent/FreeTextInput.tsx"
      provides: "Search-bar style text input for natural language intent"
      exports: ["FreeTextInput"]
    - path: "src/components/intent/IntentConfirmation.tsx"
      provides: "Confirmation card shown after intent selection with proceed button"
      exports: ["IntentConfirmation"]
    - path: "src/components/intent/IntentSection.tsx"
      provides: "Updated section with free-text input and confirmation flow"
      exports: ["IntentSection"]
  key_links:
    - from: "src/components/intent/FreeTextInput.tsx"
      to: "src/stores/intentStore.ts"
      via: "calls setFreeText and submitFreeText"
      pattern: "setFreeText|submitFreeText|useIntentStore"
    - from: "src/components/intent/IntentConfirmation.tsx"
      to: "src/stores/walletStore.ts"
      via: "checks wallet status for gating"
      pattern: "useWallet|status.*connected"
    - from: "src/components/intent/IntentConfirmation.tsx"
      to: "src/stores/intentStore.ts"
      via: "reads selected intent and provides clear action"
      pattern: "useIntentStore|selectedPresetId|freeText|clear"
    - from: "src/components/intent/IntentConfirmation.tsx"
      to: "src/components/wallet/WalletModal.tsx"
      via: "opens wallet modal when proceed clicked without connection"
      pattern: "WalletModal|walletModal|setWalletModalOpen"
---

<objective>
Add free-text natural language intent input and wallet-gated proceed flow. After selecting a preset card or submitting free text, users see a confirmation with a proceed button. Clicking proceed without a wallet prompts connection; with a wallet, it shows a "coming soon" state (Phase 3 will add the AI strategy route).

Purpose: Fulfills INTNT-02 (free-text intent input) and ONBR-02 (wallet gating only at execution, not browsing). Completes the intent capture UX.
Output: Free-text input component, confirmation flow, wallet-gated proceed action.
</objective>

<execution_context>
@/home/ahwlsqja/.claude/get-shit-done/workflows/execute-plan.md
@/home/ahwlsqja/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-intent-entry-ux/02-RESEARCH.md
@.planning/phases/02-intent-entry-ux/02-01-SUMMARY.md
@src/stores/intentStore.ts
@src/components/intent/IntentSection.tsx
@src/components/wallet/WalletModal.tsx
@src/hooks/useWallet.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create free-text input and confirmation components</name>
  <files>src/components/intent/FreeTextInput.tsx, src/components/intent/IntentConfirmation.tsx</files>
  <action>
1. Create `src/components/intent/FreeTextInput.tsx`:
   - 'use client' component
   - Search-bar style input, NOT a chat interface (PROJECT.md: "No chat UI")
   - Props: `value: string`, `onChange: (value: string) => void`, `onSubmit: () => void`, `disabled?: boolean`
   - Single `<input type="text">` with:
     - Placeholder: `무엇을 하고 싶으세요? (예: INJ가 떨어지면 분할매수)`
     - Style: `w-full rounded-2xl border border-gray-200 bg-white px-5 py-4 text-sm text-gray-900 placeholder:text-gray-400 focus:border-blue-300 focus:outline-none focus:ring-2 focus:ring-blue-100`
   - When `value.trim()` is non-empty, show a submit button inside the input (absolute positioned right):
     - Text: "확인" (Confirm)
     - Style: `rounded-lg bg-blue-600 px-3 py-1.5 text-xs font-medium text-white hover:bg-blue-700`
   - Handle Enter key to submit (only when value.trim() is non-empty)
   - When `disabled` is true, add opacity-50 and pointer-events-none

2. Create `src/components/intent/IntentConfirmation.tsx`:
   - 'use client' component
   - Displays after user selects a preset OR submits free text (when intentStore.source is not null)
   - Import useIntentStore to read: source, selectedPresetId, freeText, clear
   - Import PRESET_INTENTS from constants to look up preset title
   - Import useWallet hook to check connection status
   - Layout: a card (`rounded-2xl border border-blue-200 bg-blue-50 p-5`) showing:
     - Header: "선택한 의도" / "Selected Intent" (text-sm font-medium text-blue-900)
     - Content: If preset, show the preset's titleKo. If freetext, show the submitted freeText in quotes.
     - Two buttons row at bottom:
       a. "다시 선택" / "Change" button (text style, text-sm text-gray-500 hover:text-gray-700) -- calls intentStore.clear()
       b. "계속하기" / "Continue" button (rounded-lg bg-blue-600 px-4 py-2 text-sm font-medium text-white hover:bg-blue-700)
   - "Continue" button behavior:
     - If wallet NOT connected: open wallet modal. Use a local state `showWalletModal` and render `<WalletModal open={showWalletModal} onOpenChange={setShowWalletModal} />`. Import WalletModal from `@/components/wallet/WalletModal`.
     - If wallet IS connected: show a toast via sonner: `toast.info('전략 엔진 준비 중... (Phase 3)')` -- placeholder for Phase 3 route. Do NOT create a new route.
  </action>
  <verify>Run `npx tsc --noEmit` to confirm type correctness. Check that FreeTextInput handles Enter key and submit button. Check that IntentConfirmation imports WalletModal correctly.</verify>
  <done>FreeTextInput renders as search-bar with submit on Enter/button click. IntentConfirmation shows selected intent with change/continue actions. Continue opens wallet modal when disconnected, shows toast when connected.</done>
</task>

<task type="auto">
  <name>Task 2: Wire free-text and confirmation into IntentSection</name>
  <files>src/components/intent/IntentSection.tsx, src/app/page.tsx</files>
  <action>
1. Update `src/components/intent/IntentSection.tsx`:
   - Import FreeTextInput and IntentConfirmation
   - Import useIntentStore to read: freeText, setFreeText, submitFreeText, source
   - Layout structure:
     a. Section header (already exists from 02-01): "무엇을 하고 싶으세요?" / "What would you like to do?"
     b. FreeTextInput: placed ABOVE the card grid, connected to store's freeText/setFreeText/submitFreeText
        - Disable the input when `source !== null` (intent already selected/submitted)
     c. IntentCardGrid (already exists from 02-01)
        - When source is not null and it's a preset, visually dim the non-selected cards or keep grid as-is (keep it simple -- the confirmation below provides clarity)
     d. IntentConfirmation: rendered BELOW the grid when `source !== null`
   - Remove the placeholder comment from 02-01 about free-text input

2. Verify `src/app/page.tsx` does NOT need changes (IntentSection is already rendered unconditionally from 02-01). If page.tsx needs any minor adjustment for the flow to work, make it. The page should remain clean (~15-25 lines).

3. Ensure the full flow works:
   - User lands: sees free-text input + 6 intent cards, no wallet needed
   - User types text + Enter (or clicks submit): confirmation appears, input disabled
   - User clicks preset card: confirmation appears
   - User clicks "Change" on confirmation: resets, back to initial state
   - User clicks "Continue" without wallet: wallet modal opens
   - User clicks "Continue" with wallet: toast shows Phase 3 placeholder message
  </action>
  <verify>Run `npx tsc --noEmit` and `npm run build` to confirm compilation. Start dev server and test: (1) type free text + Enter shows confirmation, (2) select preset shows confirmation, (3) "Change" resets, (4) "Continue" without wallet opens modal, (5) "Continue" with wallet shows toast.</verify>
  <done>Complete intent entry flow: free-text input + preset cards + confirmation + wallet-gated proceed. All 4 success criteria from the roadmap are satisfied. Build passes.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with zero errors
2. `npm run build` completes successfully
3. Free-text input: type text, press Enter or click submit button -- confirmation appears
4. Preset card: click a card -- confirmation appears with card title
5. Confirmation "Change" button: clears selection, returns to initial state
6. Confirmation "Continue" without wallet: wallet connection modal opens
7. Confirmation "Continue" with wallet: toast notification appears (Phase 3 placeholder)
8. Intent browsing works entirely without wallet connection
9. Dashboard still renders correctly when wallet is connected
</verification>

<success_criteria>
- Free-text input accepts natural language and submits on Enter (INTNT-02)
- Confirmation state shows after preset selection or free-text submission
- Wallet prompted only on "Continue" action, not during browsing (ONBR-02)
- User can clear selection and try again
- No regressions in wallet connection or dashboard functionality
- Build passes with no errors
</success_criteria>

<output>
After completion, create `.planning/phases/02-intent-entry-ux/02-02-SUMMARY.md`
</output>
