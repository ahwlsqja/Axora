---
phase: 01-foundation-wallet
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - next.config.ts
  - tailwind.config.ts
  - postcss.config.mjs
  - src/app/layout.tsx
  - src/app/page.tsx
  - src/app/globals.css
  - src/services/injective/client.ts
  - src/services/injective/network.ts
  - src/services/index.ts
  - src/types/index.ts
  - src/utils/constants.ts
  - src/providers/QueryProvider.tsx
  - .env.local
  - .env.example
  - .gitignore
autonomous: true

must_haves:
  truths:
    - "Next.js app starts and renders on localhost without errors"
    - "Injective SDK client (WalletStrategy + MsgBroadcaster) is initialized and importable"
    - "Network fallback wrapper handles endpoint failures gracefully"
    - "React Query provider wraps the app for data fetching"
  artifacts:
    - path: "src/services/injective/client.ts"
      provides: "WalletStrategy singleton + MsgBroadcaster"
      exports: ["walletStrategy", "msgBroadcaster"]
    - path: "src/services/injective/network.ts"
      provides: "Network fallback utility"
      exports: ["withFallback", "getEndpoints"]
    - path: "src/utils/constants.ts"
      provides: "Chain IDs, denoms, defaults"
      contains: "AGENT_SUBACCOUNT_NONCE"
    - path: "src/types/index.ts"
      provides: "App-wide type definitions"
    - path: "src/providers/QueryProvider.tsx"
      provides: "React Query client provider"
  key_links:
    - from: "src/services/injective/client.ts"
      to: "@injectivelabs/wallet-strategy"
      via: "WalletStrategy constructor"
      pattern: "new WalletStrategy"
    - from: "src/services/injective/client.ts"
      to: "@injectivelabs/sdk-ts"
      via: "MsgBroadcaster constructor"
      pattern: "new MsgBroadcaster"
    - from: "src/app/layout.tsx"
      to: "src/providers/QueryProvider.tsx"
      via: "Provider wrapping children"
      pattern: "QueryProvider"
---

<objective>
Scaffold the Next.js project with all dependencies, configure Tailwind CSS, and set up the Injective chain client (WalletStrategy + MsgBroadcaster) with network fallback. This creates the foundation every subsequent plan builds on.

Purpose: Establish a working dev environment and Injective SDK integration so wallet connection and onboarding can be built on top.
Output: Runnable Next.js app with Injective client services ready for use.
</objective>

<execution_context>
@/home/ahwlsqja/.claude/get-shit-done/workflows/execute-plan.md
@/home/ahwlsqja/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-wallet/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Next.js project with all dependencies</name>
  <files>
    package.json
    tsconfig.json
    next.config.ts
    tailwind.config.ts
    postcss.config.mjs
    src/app/layout.tsx
    src/app/page.tsx
    src/app/globals.css
    .env.local
    .env.example
    .gitignore
  </files>
  <action>
    Create a new Next.js project in the repo root (the repo is currently empty except .planning/ and .git/).

    1. Run `npx create-next-app@latest . --typescript --tailwind --eslint --app --src-dir --no-import-alias --use-npm` to scaffold.
       - If create-next-app refuses to run in a non-empty directory, initialize manually:
         - Create package.json with next, react, react-dom
         - Create next.config.ts, tsconfig.json, tailwind.config.ts, postcss.config.mjs
         - Create src/app/layout.tsx, src/app/page.tsx, src/app/globals.css

    2. Install Injective SDK packages:
       ```
       npm install @injectivelabs/sdk-ts @injectivelabs/wallet-strategy @injectivelabs/wallet-core @injectivelabs/wallet-cosmos @injectivelabs/wallet-evm @injectivelabs/networks @injectivelabs/ts-types @injectivelabs/utils @injectivelabs/exceptions
       ```

    3. Install UI and state management:
       ```
       npm install zustand @tanstack/react-query sonner @radix-ui/react-dialog @radix-ui/react-slider
       ```

    4. Create `.env.local` with:
       ```
       NEXT_PUBLIC_NETWORK=testnet
       ```

    5. Create `.env.example` with same keys (no values) for documentation.

    6. Update `.gitignore` to include `.env.local`, `node_modules/`, `.next/`.

    7. Update `src/app/page.tsx` to show a minimal placeholder: app name "Injective Agent" centered on page.

    8. Update `src/app/layout.tsx` to set metadata title to "Injective Agent" and wrap children with the QueryProvider (created in Task 2).

    9. Configure `next.config.ts`:
       - Add `webpack` config to handle any Node.js polyfill issues from Injective SDK (common: `fs`, `net`, `tls` fallbacks set to false).
       - Set `reactStrictMode: true`.

    IMPORTANT: Use `@injectivelabs/wallet-strategy` (NOT deprecated `wallet-ts`). Use Next.js App Router (not Pages Router).
  </action>
  <verify>
    Run `npm run dev` and confirm the app starts on localhost:3000 without errors. Visit the page in a browser or curl localhost:3000 to confirm HTML is returned.
  </verify>
  <done>
    Next.js app runs on localhost:3000, all Injective SDK packages installed, Tailwind CSS configured, .env files created.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Injective client services, network fallback, types, and constants</name>
  <files>
    src/services/injective/client.ts
    src/services/injective/network.ts
    src/services/index.ts
    src/types/index.ts
    src/utils/constants.ts
    src/providers/QueryProvider.tsx
  </files>
  <action>
    1. Create `src/utils/constants.ts`:
       - `AGENT_SUBACCOUNT_NONCE = 1` (NOT 0 -- nonce 0 shares balance with bank module since v1.10.0)
       - `DEFAULT_AUTHZ_EXPIRY_SECONDS = 7 * 24 * 60 * 60` (7 days)
       - `BALANCE_POLL_INTERVAL = 10_000` (10 seconds)
       - `SPOT_MSG_TYPES` array: the 4 spot message type URLs (MsgCreateSpotLimitOrder, MsgCreateSpotMarketOrder, MsgCancelSpotOrder, MsgBatchUpdateOrders) with full protobuf paths (`/injective.exchange.v1beta1.Msg...`)
       - `DISPLAY_DENOMS` map: `{ inj: { symbol: 'INJ', decimals: 18 }, peggy0xdAC17F958D2ee523a2206206994597C13D831ec7: { symbol: 'USDT', decimals: 6 } }` (add ATOM if testnet denom is known, otherwise just INJ + USDT)
       - Network-related: export `NETWORK` derived from `process.env.NEXT_PUBLIC_NETWORK` (default 'testnet'), mapped to `Network.Testnet` or `Network.Mainnet` from `@injectivelabs/networks`

    2. Create `src/services/injective/network.ts`:
       - Import `getNetworkEndpoints` from `@injectivelabs/networks`
       - Define `FALLBACK_GRPC_ENDPOINTS` array with primary + 2 fallback endpoints for testnet
       - Export `getEndpoints()` function returning endpoints for the current network
       - Export `withFallback<T>(fn: (endpoint: string) => Promise<T>): Promise<T>` that tries each endpoint sequentially, logs warnings on failure, throws last error if all fail

    3. Create `src/services/injective/client.ts`:
       - Import WalletStrategy from `@injectivelabs/wallet-strategy`
       - Import MsgBroadcaster from `@injectivelabs/sdk-ts`
       - Import ChainId, EvmChainId from `@injectivelabs/ts-types`
       - Import NETWORK constant
       - Create and export `walletStrategy` singleton:
         ```
         chainId: ChainId.Testnet (or Mainnet based on NETWORK)
         evmOptions: { evmChainId: EvmChainId.TestnetEvm, rpcUrl from endpoints }
         ```
       - Create and export `msgBroadcaster`:
         ```
         walletStrategy, network: NETWORK, simulateTx: true
         ```
       - Guard initialization with `typeof window !== 'undefined'` to prevent SSR errors.
         Use lazy initialization pattern: export functions `getWalletStrategy()` and `getMsgBroadcaster()` that create singletons on first call, alongside direct exports for convenience.

    4. Create `src/services/index.ts`: barrel export from `./injective/client` and `./injective/network`

    5. Create `src/types/index.ts`:
       - `WalletType = 'keplr' | 'metamask'`
       - `ConnectionStatus = 'disconnected' | 'connecting' | 'connected'`
       - `OnboardingStep = 'connect' | 'explain' | 'deposit'`
       - `TokenBalance = { denom: string; symbol: string; amount: string; decimals: number }`

    6. Create `src/providers/QueryProvider.tsx`:
       - 'use client' directive
       - Create QueryClient with `defaultOptions.queries.retry: 2, staleTime: 5000`
       - Export QueryProvider component wrapping children with QueryClientProvider
       - Also wrap with Toaster from sonner (position: 'top-right')
  </action>
  <verify>
    Run `npm run build` (or `npx next build`) to confirm no TypeScript errors. Verify that `src/services/injective/client.ts` exports walletStrategy and msgBroadcaster. Run `npm run dev` and check browser console for no initialization errors.
  </verify>
  <done>
    WalletStrategy and MsgBroadcaster singletons are initialized and importable. Network fallback utility exists. Types and constants defined. React Query + Sonner providers wrap the app. Build passes without errors.
  </done>
</task>

</tasks>

<verification>
1. `npm run dev` starts without errors
2. `npm run build` completes without TypeScript errors
3. Importing `{ walletStrategy, msgBroadcaster }` from `@/services` resolves correctly
4. `src/utils/constants.ts` exports AGENT_SUBACCOUNT_NONCE = 1, SPOT_MSG_TYPES with 4 entries
5. Network fallback function is importable and handles errors gracefully
</verification>

<success_criteria>
- Next.js dev server runs on localhost:3000 showing placeholder page
- All Injective SDK packages installed and importable
- WalletStrategy + MsgBroadcaster configured for testnet
- Network fallback mechanism in place
- React Query provider active
- Build passes with zero TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-wallet/01-01-SUMMARY.md`
</output>
